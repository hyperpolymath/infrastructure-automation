# SPDX-License-Identifier: PMPL-1.0-or-later
# Ansible Inventory — infrastructure-automation
#
# Homoiconic: this inventory IS the infrastructure map.
# Each host declares its own role memberships and variables.
# The inventory describes the system; the system enacts the inventory.

all:
  children:
    silverblue:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3
          # Host-specific overrides go here
          # hostname: fedora-workstation

    # Future expansion: add remote hosts here
    # servers:
    #   hosts:
    #     web01:
    #       ansible_host: 10.0.0.10
    #     db01:
    #       ansible_host: 10.0.0.11
